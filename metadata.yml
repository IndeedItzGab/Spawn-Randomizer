name: Spawn-Randomizer
version: V1.0.6
status: STABLE
message: |
  # Spawn-Randomizer V1.0.6
  - Added `/spreadplayers` command to spread all players manually.
  - Updated administrator validation on each commands, now required players to have operator permission.